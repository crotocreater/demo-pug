{
  "name": "project3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build-css": "tailwindcss -i src/styles.css -o public/styles.css --watch",
    "build-pug": "pug -w ./src -o ./public",
    "build": "npm run build-pug && npm run build-css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "pug": "^3.0.3",
    "pug-cli": "^1.0.0-alpha6",
    "tailwindcss": "^3.4.10"
  }
}
